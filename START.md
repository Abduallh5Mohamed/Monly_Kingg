# 🚀 تشغيل المشروع - Quick Start

## تشغيل السيرفر

### الطريقة الموصى بها ✅

```bash
npm run dev
```

سيعمل على **بورت واحد فقط**: http://localhost:5000

هذا الأمر يشغل سيرفر مدمج يجمع:
- ✅ Express Backend
- ✅ Next.js Frontend  
- ✅ Socket.IO للدردشة

### طرق بديلة

#### تشغيل سيرفرات منفصلة (Frontend: 3000 / Backend: 5000):
```bash
npm run dev:separate
```

#### تشغيل Backend فقط:
```bash
npm run dev:server
```

---

## حل مشكلة "address already in use" 

إذا ظهرت رسالة `EADDRINUSE: address already in use :::5000`:

```powershell
# إيقاف جميع عمليات Node.js
Get-Process node -ErrorAction SilentlyContinue | Stop-Process -Force

# تشغيل السيرفر
npm run dev
```

---

## البورتات المستخدمة

### الوضع العادي (موصى به):
- **الموقع الكامل**: http://localhost:5000

### الوضع المنفصل:
- **Frontend (Next.js)**: http://localhost:3000
- **Backend (Express)**: http://localhost:5000

### قواعد البيانات:
- **MongoDB**: localhost:27017
- **Redis**: localhost:6379

---

## Scripts المتاحة

| Command | Description |
|---------|-------------|
| `npm run dev` | 🚀 تشغيل السيرفر المدمج على بورت 5000 (موصى به) |
| `npm run dev:separate` | تشغيل سيرفرين منفصلين (3000 + 5000) |
| `npm run dev:server` | تشغيل Express Backend فقط (5000) |
| `npm run dev:next` | تشغيل Next.js Frontend فقط (3000) |
| `npm run build` | بناء Next.js للإنتاج |
| `npm start` | تشغيل الإنتاج (بعد البناء) |
| `npm run cleanup` | تنظيف الملفات المؤقتة |

---

## ملاحظات مهمة

1. ✅ استخدم `npm run dev` للتشغيل البسيط على بورت واحد (5000)
2. 🔄 السيرفر سيعيد التشغيل تلقائياً عند تغيير الملفات
3. 🌐 افتح http://localhost:5000 في المتصفح
